<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ExamenProgresoDosEmilioGuerrero.GridPage"
             Title="GridPage">
    <Grid
        RowDefinitions="*,*"
              ColumnDefinitions="*,*" 
              >
        <Label
            Grid.Row="0"
            Grid.Column="0"
            Text="Emilio"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            >
            
        </Label>
        <Label
            Grid.Row="0"
            Grid.Column="1"
            Text="Esteban"
            VerticalOptions="Center"
            HorizontalOptions="Center"
           >

        </Label>
        <Label
            Grid.Row="1"
            Grid.Column="0"
            Text="Guerrero"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            >

        </Label>
        <Label
            Grid.Row="1"
            Grid.Column="1"
            Text="Argoti"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            >

        </Label>



    </Grid>
</ContentPage>